# 아키텍처

## 목표
{이 구조가 달성해야 하는 개발 목표를 적는다. 예: 기능별 변경 범위가 작고, 테스트하기 쉬우며, API 경계가 명확해야 한다.}

## 디렉토리 구조
```
src/
├── app/               # 페이지 + API 라우트
├── components/        # UI 컴포넌트
├── types/             # TypeScript 타입 정의
├── lib/               # 유틸리티 + 헬퍼
└── services/          # 외부 API 래퍼
```

## 패턴
{사용하는 디자인 패턴을 적는다. 예: Server Components 기본, 인터랙션이 필요한 곳만 Client Component}

## 데이터 흐름
```
{데이터가 어떻게 흐르는지 (예:
사용자 입력 → Client Component → API Route → 외부 API → 응답 → UI 업데이트
)}
```

## 상태 관리
{상태 관리 방식 (예: 서버 상태는 Server Components, 클라이언트 상태는 useState/useReducer)}

## API 경계
- {클라이언트가 직접 호출해도 되는 것}
- {서버/API 라우트에서만 처리해야 하는 것}
- {외부 API 키나 비밀값을 다루는 위치}

## 테스트 전략
- 단위 테스트: {순수 함수, 파서, 변환 로직 등}
- 통합 테스트: {API route, service wrapper 등}
- UI 테스트: {주요 사용자 흐름, 접근성 등}

## 에러 처리
- {사용자 입력 오류 처리}
- {외부 API 실패 처리}
- {재시도 또는 fallback 정책}

## 금지할 구조
- {예: UI 컴포넌트에서 외부 API 직접 호출 금지. 이유: API 키 노출 및 테스트 어려움}
- {예: 공통 유틸에 도메인 로직 섞기 금지. 이유: 변경 영향 추적 어려움}
